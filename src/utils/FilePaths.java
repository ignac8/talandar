package utils;

public class FilePaths {
    private static String RES = "res\\";

    private static String DATA = "data\\";

    private static String ARRAYS = "arrays\\";

    private static String CLASSES = "classes\\";

    private static String JSON = ".json";

    public static String RACE_TYPES_ARRAY = RES + DATA + ARRAYS + "raceTypes" + JSON;
    public static String BULLET_TYPES_ARRAY = RES + DATA + ARRAYS + "bulletTypes" + JSON;
    public static String DAMAGE_TYPES_ARRAY = RES + DATA + ARRAYS + "damageTypes" + JSON;
    public static String EXPLOSION_TYPES_ARRAY = RES + DATA + ARRAYS + "explosionTypes" + JSON;
    public static String ORDER_TYPES_ARRAY = RES + DATA + ARRAYS + "orderTypes" + JSON;
    public static String TECH_TYPES_ARRAY = RES + DATA + ARRAYS + "techTypes" + JSON;
    public static String UNIT_COMMAND_TYPES_ARRAY = RES + DATA + ARRAYS + "unitCommandTypes" + JSON;
    public static String UNIT_SIZE_TYPES_ARRAY = RES + DATA + ARRAYS + "unitSizeTypes" + JSON;
    public static String UNIT_TYPES_ARRAY = RES + DATA + ARRAYS + "unitTypes" + JSON;
    public static String UPGRADE_TYPES_ARRAY = RES + DATA + ARRAYS + "upgradeTypes" + JSON;
    public static String WEAPON_TYPES_ARRAY = RES + DATA + ARRAYS + "weaponTypes" + JSON;

    public static String RACE_TYPES_CLASS = RES + DATA + CLASSES + "raceTypes" + JSON;
    public static String BULLET_TYPES_CLASS = RES + DATA + CLASSES + "bulletTypes" + JSON;
    public static String DAMAGE_TYPES_CLASS = RES + DATA + CLASSES + "damageTypes" + JSON;
    public static String EXPLOSION_TYPES_CLASS = RES + DATA + CLASSES + "explosionTypes" + JSON;
    public static String ORDER_TYPES_CLASS = RES + DATA + CLASSES + "orderTypes" + JSON;
    public static String TECH_TYPES_CLASS = RES + DATA + CLASSES + "techTypes" + JSON;
    public static String UNIT_COMMAND_TYPES_CLASS = RES + DATA + CLASSES + "unitCommandTypes" + JSON;
    public static String UNIT_SIZE_TYPES_CLASS = RES + DATA + CLASSES + "unitSizeTypes" + JSON;
    public static String UNIT_TYPES_CLASS = RES + DATA + CLASSES + "unitTypes" + JSON;
    public static String UPGRADE_TYPES_CLASS = RES + DATA + CLASSES + "upgradeTypes" + JSON;
    public static String WEAPON_TYPES_CLASS = RES + DATA + CLASSES + "weaponTypes" + JSON;
}
