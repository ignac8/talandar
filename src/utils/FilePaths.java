package utils;

public class FilePaths {
    private static final String RES = "res\\";

    private static final String DATA = "data\\";

    private static final String ARRAYS = "arrays\\";

    private static final String CLASSES = "classes\\";

    private static final String JSON = ".json";

    public static final String RACE_TYPES_ARRAY = RES + DATA + ARRAYS + "raceTypes" + JSON;
    public static final String BULLET_TYPES_ARRAY = RES + DATA + ARRAYS + "bulletTypes" + JSON;
    public static final String DAMAGE_TYPES_ARRAY = RES + DATA + ARRAYS + "damageTypes" + JSON;
    public static final String EXPLOSION_TYPES_ARRAY = RES + DATA + ARRAYS + "explosionTypes" + JSON;
    public static final String ORDER_TYPES_ARRAY = RES + DATA + ARRAYS + "orderTypes" + JSON;
    public static final String TECH_TYPES_ARRAY = RES + DATA + ARRAYS + "techTypes" + JSON;
    public static final String UNIT_COMMAND_TYPES_ARRAY = RES + DATA + ARRAYS + "unitCommandTypes" + JSON;
    public static final String UNIT_SIZE_TYPES_ARRAY = RES + DATA + ARRAYS + "unitSizeTypes" + JSON;
    public static final String UNIT_TYPES_ARRAY = RES + DATA + ARRAYS + "unitTypes" + JSON;
    public static final String UPGRADE_TYPES_ARRAY = RES + DATA + ARRAYS + "upgradeTypes" + JSON;
    public static final String WEAPON_TYPES_ARRAY = RES + DATA + ARRAYS + "weaponTypes" + JSON;

    public static final String RACE_TYPES_CLASS = RES + DATA + CLASSES + "raceTypes" + JSON;
    public static final String BULLET_TYPES_CLASS = RES + DATA + CLASSES + "bulletTypes" + JSON;
    public static final String DAMAGE_TYPES_CLASS = RES + DATA + CLASSES + "damageTypes" + JSON;
    public static final String EXPLOSION_TYPES_CLASS = RES + DATA + CLASSES + "explosionTypes" + JSON;
    public static final String ORDER_TYPES_CLASS = RES + DATA + CLASSES + "orderTypes" + JSON;
    public static final String TECH_TYPES_CLASS = RES + DATA + CLASSES + "techTypes" + JSON;
    public static final String UNIT_COMMAND_TYPES_CLASS = RES + DATA + CLASSES + "unitCommandTypes" + JSON;
    public static final String UNIT_SIZE_TYPES_CLASS = RES + DATA + CLASSES + "unitSizeTypes" + JSON;
    public static final String UNIT_TYPES_CLASS = RES + DATA + CLASSES + "unitTypes" + JSON;
    public static final String UPGRADE_TYPES_CLASS = RES + DATA + CLASSES + "upgradeTypes" + JSON;
    public static final String WEAPON_TYPES_CLASS = RES + DATA + CLASSES + "weaponTypes" + JSON;
}
